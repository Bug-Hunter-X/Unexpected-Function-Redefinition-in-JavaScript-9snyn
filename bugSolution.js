// Avoid redefining the function by using a different name or checking if the function exists before defining it. 
function add(a,b){return a+b;}
console.log(add(2,3)); //5

function subtract(a,b){return a-b;}
console.log(subtract(2,3)); //-1